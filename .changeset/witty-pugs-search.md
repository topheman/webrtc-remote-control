---
---

Move the unit suites from Jest to Vitest. No change to the published package contents:
only test configuration, test-only dependencies and the scripts that run them. The
end-to-end suite stays on Jest until it moves to Playwright.
