{
  "name": "@webrtc-remote-control/demo",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5000",
    "forward:dev": "ssh -R 80:localhost:3000 localhost.run",
    "forward:preview": "ssh -R 80:localhost:5000 localhost.run",
    "test": "jest",
    "test:precommit": "jest --bail --findRelatedTests",
    "test:watch": "jest --watch -o",
    "test:e2e": "jest --config ./jest.e2e.config.js",
    "test:e2e:watch": "jest --config ./jest.e2e.config.js --watch -o",
    "test:e2e:headless-false": "HEADLESS=false jest --config ./jest.e2e.config.js"
  },
  "author": "Christophe Rosset <tophe@topheman.com> (http://labs.topheman.com/)",
  "license": "ISC",
  "devDependencies": {
    "@babel/preset-env": "^7.16.11",
    "@vitejs/plugin-react": "^1.2.0",
    "jest": "^27.5.1",
    "jest-cucumber": "^3.0.1",
    "jest-puppeteer": "^6.1.0",
    "vite": "^2.8.3"
  },
  "dependencies": {
    "@webrtc-remote-control/core": "^0.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
